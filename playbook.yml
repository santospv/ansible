- hosts: webserver
  tasks:
    - name: Coleta o hostname
      command: hostname

    - name: Instalar Apache 2
      become: yes
      apt: 
        name: apache2
        state: absent
        update_cache: yes
